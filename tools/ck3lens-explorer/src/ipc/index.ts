/**
 * IPC Module - Inter-Process Communication
 * 
 * Provides TCP-based IPC server for external tools (like MCP server)
 * to access VS Code APIs such as diagnostics, open files, etc.
 */

export { DiagnosticsServer } from './diagnosticsServer';
